---
expect:
  names: array
  reason: string
  usage: [interactive, unattended]
---

Stored {{input.names}} in encrypted (project). No values were shown to you; they were entered by the user out of band.
